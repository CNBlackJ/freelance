
<%= form_for @category, html: { class: "form-horizontal", id: "edit_post_45" } do |f| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :cat_key, class: 'col-md-2 control-label', for: 'cat_key' %>
    <div class="col-sm-10">
      <%= f.text_field :cat_key, class: 'form-control', id: 'cat_key', placaholder: 'category key' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :cat_name, class: 'col-md-2 control-label', for: 'cat_name' %>
    <div class="col-sm-10">
      <%= f.text_field :cat_name, class: 'form-control', id: 'cat_name', placaholder: 'cat_name' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit 'Save', class: 'btn btn-success' %>
    </div>
  </div>
<% end %>
